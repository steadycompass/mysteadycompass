# Copy this file to secrets.toml (same folder) and paste your key.
# Never commit secrets.toml or paste the key inside your Python code.
# Free API key: https://aistudio.google.com/apikey
#
# In secrets.toml add one line (replace the value with your real key):
#   GEMINI_API_KEY = "paste-your-key-here"
#
# On Streamlit Cloud: use Settings -> Secrets and add GEMINI_API_KEY there.
# Used for: Weekly Signal, Current Sea (Compass Wisdom), Guru's Wisdom via Gemini 1.5 Flash.
